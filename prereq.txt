DEFAULT SETTING
1. capslock -> ctrl

	tweak -> keyboard&mouse -> Additional Layout Options -> Ctrl pposition -> Caps Lcok as Ctrl

2. zsh
	
	sudo apt install zsh

	sh -c "$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
	^^ github put source or forK?

	chsh -s $(which zsh)

	theme later

3. wal
	git wal script -> /usr/local/bin



4. tmux
	sudo apt install tmux
	add this code into .zshrc
	"""
	if command -v tmux &> /dev/null && [ -n "$PS1" ] && [[ ! "$TERM" =~ screen ]] && [[ ! "$TERM" =~ tmux ]] && [ -z "$TMUX" ]; then
	  exec tmux
	fi
	"""

	
	
5. tilda drop down terminal




